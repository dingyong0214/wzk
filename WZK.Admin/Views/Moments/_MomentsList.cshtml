@{
    Layout = null;
}
<style>
    .img-margin {
        margin-right: 5px;
        margin-top: 5px;
        margin-bottom: 5px;
    }
</style>
<div class="widget-content nopadding">
    <table class="table table-bordered table-striped with-check table-hover">
        <thead>
            <tr>
                <th class="w20">标题</th>
                <th class="w35">内容</th>
                <th class="w15">录入人</th>
                <th class="w20">录入时间</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            @if (ViewBag.List.Count == 0)
            {
                <tr class="gradeA">
                    <td colspan="6">未查询到相关数据</td>
                </tr>
            }
            else
            {
                foreach (WZK.Models.admin.MMoments item in ViewBag.List.ReturnList)
                {
                    <tr class="gradeA">
                        <td>@item.Title</td>
                        <td>@item.Content</td>
                        <td>@item.InputUser</td>
                        <td>@item.InsertTime.ToString("yyyy-MM-dd HH:mm:ss")</td>
                        <td>
                            <div class="xc_wrap">
                                <a href="javascript:;" data-id="@item.Id" data-name="@item.Title" class="btn btn-info xc btn-mini js-choice">选择</a>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
@if ((int)ViewBag.List.Count > WZK.Common.ConfigData.PageConfig.PageSize)
{
    <div class="pagination page">
        @Html.ShowPageNavigate((int)ViewBag.List.Count, WZK.Common.ConfigData.PageConfig.PageSize)
    </div>
}